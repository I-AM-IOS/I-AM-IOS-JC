{
  "name": "i-am-ios-dmr-upgrade",
  "version": "2.1.0",
  "description": "I-AM-IOS v2 — Deterministic Multi-Path Routing (DMR) upgrade",
  "main": "src/routing/overlay-routing.ts",
  "scripts": {
    "test": "ts-node tests/overlay.test.ts",
    "build": "tsc --target ESNext --module ESNext --moduleResolution bundler --outDir dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^11.0.0",
    "chalk": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  }
}
