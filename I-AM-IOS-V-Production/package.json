{
  "name": "sovereign-compute-network",
  "version": "2.0.0",
  "description": "Sovereign substrate — deterministic event-driven OS: rekernel + WASM kernel + JSONFlow + P2P fabric",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start":          "node server.js",
    "dev":            "node server.js 3000",
    "test:kernel":    "node test-kernel.js",
    "build:rekernel": "cd rekernel && npx tsc --target ESNext --module ESNext --moduleResolution bundler --outDir ../rekernel-dist --declaration --strict 2>&1 || true",
    "build:wasm":     "cd wasm-kernel && cargo build --release --target wasm32-unknown-unknown",
    "test:wasm":      "cd wasm-kernel && cargo test",
    "run:wasm":       "cd wasm-kernel && cargo run --release",
    "setup":          "rustup target add wasm32-unknown-unknown"
  },
  "engines": { "node": ">=18.0.0" },
  "license": "Sovereign OS Community License"
}
